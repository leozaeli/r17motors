import { createClient } from 'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm';
const supabase=createClient(window.R17_SUPABASE_URL,window.R17_SUPABASE_KEY),grid=document.querySelector('#catalog-grid'),count=document.querySelector('.catalog-count'),money=v=>v?new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL',maximumFractionDigits:0}).format(v):'Sob consulta',safe=v=>String(v||'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
async function loadCatalog(){const {data:vehicles,error}=await supabase.from('vehicles').select('*, vehicle_images(path,alt,position)').eq('status','published').order('featured',{ascending:false}).order('created_at',{ascending:false});if(error||!vehicles?.length)return;count.textContent=`${String(vehicles.length).padStart(2,'0')} veículo${vehicles.length>1?'s':''} disponível${vehicles.length>1?'is':''}`;grid.innerHTML=vehicles.map(v=>{const image=[...(v.vehicle_images||[])].sort((a,b)=>a.position-b.position)[0],url=image?supabase.storage.from('vehicle-media').getPublicUrl(image.path).data.publicUrl:'refs/bmw/WhatsApp%20Image%202026-09-13%20at%2020.48.24%20(4).jpeg';return `<article class="catalog-card"><div class="catalog-image"><img src="${url}" alt="${safe(image?.alt||`${v.brand} ${v.model}`)}"><span>Disponível</span></div><div class="catalog-info"><div><p>${safe(v.brand)}</p><h2>${safe(v.model)}</h2></div><p class="catalog-description">${safe(v.description||`${v.year||''} ${v.version||''}`)}</p><div class="catalog-meta"><span>${safe(v.year||'Detalhes')} · ${money(v.price)}</span><button type="button" data-open-contact>Tenho interesse <b>↗</b></button></div></div></article>`}).join('')}
loadCatalog();
