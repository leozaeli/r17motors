import{createClient}from'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm';
const db=createClient(window.R17_SUPABASE_URL,window.R17_SUPABASE_KEY),slug=new URLSearchParams(location.search).get('slug'),page=document.querySelector('#vehicle-page'),money=v=>v?new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL',maximumFractionDigits:0}).format(v):'Sob consulta',safe=v=>String(v||'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const{data:v}=await db.from('vehicles').select('*,vehicle_images(path,alt,position)').eq('slug',slug).eq('status','published').single();
if(!v)page.innerHTML='<p>Veículo não encontrado.</p>';else{const image=[...(v.vehicle_images||[])].sort((a,b)=>a.position-b.position)[0],url=image?db.storage.from('vehicle-media').getPublicUrl(image.path).data.publicUrl:'logo/r17motors-transparent.png',name=`${v.brand} ${v.model}`,link=location.href,message=`Olá, R17 Motors! Tenho interesse no ${name}.\n\nLink do veículo: ${link}\n\nGostaria de receber mais informações.`,specs=[v.year,v.mileage?`${new Intl.NumberFormat('pt-BR').format(v.mileage)} km`:null,v.transmission,v.color].filter(Boolean);page.innerHTML=`<article class="catalog-card"><div class="catalog-image"><img src="${url}" alt="${safe(name)}"></div><div class="catalog-info"><div><p>${safe(v.brand)}</p><h2>${safe(v.model)}</h2></div><p class="catalog-description">${safe(v.description||'')}</p><div class="vehicle-specs">${specs.map(s=>`<span>${safe(s)}</span>`).join('')}</div><a class="vehicle-interest" target="_blank" rel="noreferrer" href="https://wa.me/5571991954004?text=${encodeURIComponent(message)}">Tenho interesse <span>↗</span></a><div class="catalog-meta"><span>${v.year||''} · ${money(v.price)}</span><a href="/veiculos">Voltar ao catálogo</a></div></div></article>`}
