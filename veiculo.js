import{createClient}from'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm';const db=createClient(window.R17_SUPABASE_URL,window.R17_SUPABASE_KEY),slug=new URLSearchParams(location.search).get('slug'),page=document.querySelector('#vehicle-page'),money=v=>v?new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL',maximumFractionDigits:0}).format(v):'Sob consulta';const{data:v}=await db.from('vehicles').select('*,vehicle_images(path,alt,position)').eq('slug',slug).eq('status','published').single();if(!v)page.innerHTML='<p>Veículo não encontrado.</p>';else{const image=[...(v.vehicle_images||[])].sort((a,b)=>a.position-b.position)[0],url=image?db.storage.from('vehicle-media').getPublicUrl(image.path).data.publicUrl:'logo/r17motors-transparent.png';page.innerHTML=`<article class="catalog-card"><div class="catalog-image"><img src="${url}" alt="${v.brand} ${v.model}"></div><div class="catalog-info"><div><p>${v.brand}</p><h2>${v.model}</h2></div><p class="catalog-description">${v.description||''}</p><div class="catalog-meta"><span>${v.year||''} · ${money(v.price)}</span><a href="/veiculos">Voltar ao catálogo</a></div></div></article>`}
