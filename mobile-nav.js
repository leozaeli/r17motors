const toggle=document.querySelector('.mobile-menu-toggle'),menu=document.querySelector('.mobile-nav');if(toggle&&menu){const set=open=>{document.body.classList.toggle('menu-open',open);toggle.setAttribute('aria-expanded',String(open));toggle.setAttribute('aria-label',open?'Fechar menu':'Abrir menu')};toggle.addEventListener('click',()=>set(!document.body.classList.contains('menu-open')));menu.addEventListener('click',event=>{if(event.target===menu)set(false)});menu.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>set(false)));document.addEventListener('keydown',event=>{if(event.key==='Escape')set(false)})}
